import { MainTable.Datasource } from './main-table.datasource';

describe('MainTable.Datasource', () => {
  it('should create an instance', () => {
    expect(new MainTable.Datasource()).toBeTruthy();
  });
});
